<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".GroupChatActivity"
    android:orientation="vertical">

    <!-- Header: Group Avatar + Group Name -->
    <LinearLayout
        android:id="@+id/layout_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp"
        android:background="#EEEEEE"
        android:elevation="2dp">

        <!-- Group Avatar -->
        <ImageView
            android:id="@+id/imageViewGroupAvatar"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/default_avatar"
            android:scaleType="centerCrop" />

        <!-- Group Name (EditText disabled) -->
        <EditText
            android:id="@+id/textViewGroupName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_gravity="center_vertical"
            android:enabled="false"
            android:textSize="18sp"
            android:textColor="@color/black"
            android:background="@null"
            android:text="Group Name" />
    </LinearLayout>

    <!-- Messages RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewChat"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="8dp"
        android:clipToPadding="false" />

    <!-- Reply Bar (initially hidden) -->
    <LinearLayout
        android:id="@+id/reply_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:padding="8dp"
        android:orientation="horizontal"
        android:background="@color/offwhite"
        android:elevation="4dp">

        <TextView
            android:id="@+id/replybar_username"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="User"
            android:textStyle="bold"
            android:textColor="@color/black" />

        <TextView
            android:id="@+id/replybar_message"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1"
            android:paddingStart="8dp"
            android:text="Original message…"
            android:textColor="@color/black" />

        <ImageButton
            android:id="@+id/replybar_close"
            android:layout_width="15dp"
            android:layout_height="15dp"
            android:contentDescription="Close"
            android:background="@drawable/close_ic"
            android:backgroundTint="@color/black"
            android:layout_marginTop="4dp" />
    </LinearLayout>

    <!-- Input Area: CardView containing optional image preview + text input row -->
    <androidx.cardview.widget.CardView
        android:id="@+id/messageInputLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:elevation="8dp"
        app:cardCornerRadius="8dp"
        android:backgroundTint="@color/offwhite"
        android:padding="8dp">

        <!-- Vertical container for preview + input row -->
        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- Preview image (initially hidden) -->
            <ImageView
                android:id="@+id/prewImageView"
                android:layout_width="400dp"
                android:layout_height="200dp"
                android:layout_gravity="center_horizontal"
                android:layout_marginBottom="8dp"
                android:adjustViewBounds="true"
                android:visibility="gone" />

            <!-- Input row: attachment button, EditText, send button -->
            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingVertical="4dp">

                <!-- Attach button (click handler can call a method to pick file) -->
                <androidx.appcompat.widget.AppCompatButton
                    android:id="@+id/imageviewattachment"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:background="@drawable/plus_ic"
                    android:contentDescription="Attach"
                    android:onClick="openFilePicker"
                    android:layout_alignParentStart="true"
                    android:layout_centerVertical="true" />

                <!-- Send button -->
                <androidx.appcompat.widget.AppCompatButton
                    android:id="@+id/send_btn"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:background="@drawable/sent_icon"
                    android:contentDescription="Send"
                    android:layout_alignParentEnd="true"
                    android:layout_centerVertical="true" />

                <!-- Message EditText (fills space) -->
                <EditText
                    android:id="@+id/editTextMessage"
                    android:layout_width="match_parent"
                    android:layout_height="40dp"
                    android:layout_toEndOf="@id/imageviewattachment"
                    android:layout_toStartOf="@id/send_btn"
                    android:layout_marginStart="8dp"
                    android:layout_marginEnd="8dp"
                    android:background="@drawable/input_bg"
                    android:hint="Type a message"
                    android:paddingStart="12dp"
                    android:paddingEnd="12dp"
                    android:textColor="@color/black"
                    android:textColorHint="@color/inactive_gray" />
            </RelativeLayout>
        </LinearLayout>
    </androidx.cardview.widget.CardView>
</LinearLayout>
